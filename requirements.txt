# ─────────────────────────────────────────────
# hallu-check  |  requirements.txt
#
# All LangChain packages are pinned to the 0.3.x generation so they
# all share langchain-core>=0.3 — eliminating the version triangle that
# caused the previous ResolutionImpossible error.
#
# Patch-level version pins have been relaxed so pip can download
# pre-built wheels for Python 3.14 instead of compiling from source.
# ─────────────────────────────────────────────

# ── Web Framework ────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-dotenv>=1.0.1
pydantic>=2.7.0

# ── LangChain core (shared by all lc-* packages) ──
# All three integrations below require langchain-core>=0.3, <0.4
langchain>=0.3.0,<0.4
langchain-core>=0.3.0,<0.4

# ── LLM – Primary Generator (Qwen via HuggingFace Inference API) ──
# 0.1.x series targets langchain-core 0.3.x
langchain-huggingface>=0.1.0,<0.2
huggingface-hub>=0.23.0
transformers>=4.41.0
requests>=2.32.0


# ── LLM – Judge / Refiner (Gemini via Google AI Studio) ──────────
# Use new Gemini SDK as recommended
google-genai>=0.5.0

# ── Web Search & Scraping ────────────────────
googlesearch-python>=1.2.3
httpx>=0.27.0
beautifulsoup4>=4.12.0
lxml>=5.0.0

# ── PageIndex (Vectorless Reasoning RAG) ─────
# PageIndex is vendored (see nodes/pageindex_rag.py).
# tiktoken is its only extra dependency.
tiktoken>=0.7.0

# ── NLP Metrics ──────────────────────────────
nltk>=3.8.1

# ── NLI Model (DeBERTa-v3 trained on MNLI) ──
# Replaces bert-score with proper NLI-based verification
datasets>=2.19.0
accelerate>=0.30.0
evaluate>=0.4.0
scikit-learn>=1.4.0

# ── Utilities ────────────────────────────────
aiofiles>=23.2.1
tenacity>=8.3.0

# ── Protobuf (Python 3.14 compatibility) ─────
# Pinned to 5.x for compatibility with google-ai-generativelanguage
# and to avoid C extension issues on Python 3.14
protobuf>=5.28.0,<6.0.0

